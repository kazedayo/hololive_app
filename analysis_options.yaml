include: package:flutter_lints/flutter.yaml

analyzer:
  exclude: 
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/generated/**"
linter:
  rules:
    # non_constant_identifier_names: false
    # avoid_void_async: false